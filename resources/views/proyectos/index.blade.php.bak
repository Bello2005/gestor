@extends('layouts.main')

@section('title', 'Estructura de Proyectos')

@section('styles')
    <!-- DataTables CSS -->
    <link rel="stylesheet" href="https://cdn.datatables.net/1.11.5/css/dataTables.bootstrap5.min.css">
    
    <style>
        /* Estilos para las tarjetas de estadísticas */
        .stat-card {
            transition: transform 0.2s, box-shadow 0.2s;
            border: none;
            box-shadow: var(--card-shadow);
        }

        .stat-card:hover {
            transform: translateY(-5px);
            box-shadow: var(--card-shadow-hover);
        }

        .stat-card .icon-container {
            width: 48px;
            height: 48px;
            border-radius: 12px;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .stat-card .icon-container i {
            font-size: 1.5rem;
        }

        .stat-card .card-title {
            font-size: 1.75rem;
            font-weight: 600;
        }

        .stat-card .card-subtitle {
            font-size: 0.875rem;
            font-weight: 500;
        }

        /* Estilos para la tabla */
        #proyectosTable {
            width: 100% !important;
            margin-bottom: 1rem;
            border-collapse: collapse;
        }
        
        #proyectosTable th,
        #proyectosTable td {
            padding: .75rem;
            vertical-align: middle;
        }
        
        #proyectosTable thead th {
            border-bottom: 2px solid #dee2e6;
            background-color: #f8f9fa;
            white-space: nowrap;
        }
        
        .btn-group-action {
            display: flex;
            gap: 0.5rem;
            justify-content: center;
        }
        
        .btn-icon {
            padding: 0.375rem;
            line-height: 1;
        }

        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(10px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes shimmer {
            0% { background-position: -1000px 0; }
            100% { background-position: 1000px 0; }
        }

        .loading tbody tr {
            position: relative;
            overflow: hidden;
        }

        .loading tbody tr::after {
            content: '';
            position: absolute;
            inset: 0;
            background: linear-gradient(90deg, 
                transparent,
                rgba(255,255,255,0.3),
                transparent);
            animation: shimmer 2s infinite linear;
        }

        .highlight {
            transform: scale(1.01);
            z-index: 1;
        }

        .dim {
            opacity: 0.7;
        }

        .ripple {
            position: absolute;
            border-radius: 50%;
            transform: scale(0);
            animation: ripple 0.6s linear;
            background-color: rgba(255,255,255,0.7);
            width: 100px;
            height: 100px;
            margin-left: -50px;
            margin-top: -50px;
            pointer-events: none;
        }

        @keyframes ripple {
            to {
                transform: scale(4);
                opacity: 0;
            }
        }

        .btn {
            position: relative;
            overflow: hidden;
        }
    </style>
@endsection

@section('content')
<!-- Main Content -->
<div class="content-wrapper">
    <!-- Header Section -->
    <div class="row align-items-center mb-4">
        <div class="col-12">
            <div class="d-flex justify-content-between align-items-center">
                <h1 class="h3 mb-0">Sistema de Gestión de Proyectos</h1>
                <div class="d-flex align-items-center gap-3">
                    <a href="{{ route('proyectos.create') }}" class="btn btn-primary">
                        <i class="fas fa-plus-circle me-2"></i>Nuevo Proyecto
                    </a>
                </div>
            </div>
        </div>
    </div>

    <!-- Statistics Cards -->
    <div class="row g-4 mb-4">
        <!-- Total Proyectos -->
        <div class="col-12 col-sm-6 col-xl-3">
            <div class="card stat-card">
                <div class="card-body">
                    <div class="d-flex align-items-center">
                        <div class="icon-container bg-primary-subtle">
                            <i class="fas fa-folder-open text-primary"></i>
                        </div>
                        <div class="ms-3">
                            <h6 class="card-subtitle text-muted mb-1">Total Proyectos</h6>
                            <h2 class="card-title mb-0">{{ $proyectos->count() }}</h2>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Proyectos Activos -->
        <div class="col-12 col-sm-6 col-xl-3">
            <div class="card stat-card">
                <div class="card-body">
                    <div class="d-flex align-items-center">
                        <div class="icon-container bg-success-subtle">
                            <i class="fas fa-check-circle text-success"></i>
                        </div>
                        <div class="ms-3">
                            <h6 class="card-subtitle text-muted mb-1">Proyectos Activos</h6>
                            <h2 class="card-title mb-0">{{ $proyectos->where('estado', 'activo')->count() }}</h2>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Valor Total -->
        <div class="col-12 col-sm-6 col-xl-3">
            <div class="card stat-card">
                <div class="card-body">
                    <div class="d-flex align-items-center">
                        <div class="icon-container bg-warning-subtle">
                            <i class="fas fa-dollar-sign text-warning"></i>
                        </div>
                        <div class="ms-3">
                            <h6 class="card-subtitle text-muted mb-1">Valor Total</h6>
                            <h2 class="card-title mb-0">${{ number_format($proyectos->sum('valor_total'), 0, ',', '.') }}</h2>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Entidades -->
        <div class="col-12 col-sm-6 col-xl-3">
            <div class="card stat-card">
                <div class="card-body">
                    <div class="d-flex align-items-center">
                        <div class="icon-container bg-info-subtle">
                            <i class="fas fa-building text-info"></i>
                        </div>
                        <div class="ms-3">
                            <h6 class="card-subtitle text-muted mb-1">Entidades</h6>
                            <h2 class="card-title mb-0">{{ $proyectos->unique('entidad_contratante')->count() }}</h2>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Main Table -->
    <div class="card shadow-sm">
        <div class="card-header bg-white py-3">
            <div class="row align-items-center">
                <div class="col">
                    <h5 class="mb-0 text-dark">Listado de Proyectos</h5>
                </div>
                <div class="col-auto">
                    <div class="form-check form-switch">
                        <input class="form-check-input" type="checkbox" id="showInactiveProjects">
                        <label class="form-check-label" for="showInactiveProjects">Mostrar inactivos</label>
                    </div>
                </div>
            </div>
        </div>
        <div class="card-body">
            <div class="table-responsive">
                <table id="proyectosTable" class="table table-hover align-middle">
                    <thead>
                        <tr>
                            <th>ID</th>
                            <th style="min-width: 250px;">Nombre del Proyecto</th>
                            <th style="min-width: 200px;">Entidad Contratante</th>
                            <th>Fecha de Ejecución</th>
                            <th>Plazo (Meses)</th>
                            <th>Valor Total</th>
                            <th>Estado</th>
                            <th style="width: 120px;">Acciones</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach($proyectos as $proyecto)
                            <tr>
                                <td>{{ $proyecto->id }}</td>
                                <td>{{ $proyecto->nombre_del_proyecto }}</td>
                                <td>{{ $proyecto->entidad_contratante }}</td>
                                <td>{{ $proyecto->fecha_de_ejecucion ? $proyecto->fecha_de_ejecucion->format('d/m/Y') : 'N/A' }}</td>
                                <td>{{ $proyecto->plazo ?? 'N/A' }}</td>
                                <td>${{ number_format($proyecto->valor_total ?? 0, 2, ',', '.') }}</td>
                                <td>
                                    <span class="d-flex align-items-center">
                                        <i class="fas fa-circle" style="color: {{ $proyecto->estado === 'activo' ? '#22c55e' : ($proyecto->estado === 'inactivo' ? '#6b7280' : '#ef4444') }}; font-size: 8px; margin-right: 8px;"></i>
                                        {{ ucfirst($proyecto->estado) }}
                                    </span>
                                </td>
                                <td>
                                    <div class="btn-group-action">
                                        <a href="{{ route('proyectos.edit', $proyecto->id) }}" class="btn btn-sm btn-primary btn-icon">
                                            <i class="fas fa-edit"></i>
                                        </a>
                                        <form action="{{ route('proyectos.destroy', $proyecto->id) }}" method="POST" class="d-inline">
                                            @csrf
                                            @method('DELETE')
                                            <button type="submit" class="btn btn-sm btn-danger btn-icon" onclick="return confirm('¿Está seguro de eliminar este proyecto?')">
                                                <i class="fas fa-trash-alt"></i>
                                            </button>
                                        </form>
                                    </div>
                                </td>
                            </tr>
                        @endforeach
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
@endsection

@section('scripts')
    <!-- DataTables -->
    <script src="https://cdn.datatables.net/1.11.5/js/jquery.dataTables.min.js"></script>
    <script src="https://cdn.datatables.net/1.11.5/js/dataTables.bootstrap5.min.js"></script>
    
    <script>
        $(document).ready(function() {
            // Animación de entrada para las tarjetas de estadísticas
            $('.stat-card').each(function(index) {
                $(this).css({
                    'opacity': '0',
                    'transform': 'translateY(20px)'
                }).delay(index * 100).animate({
                    'opacity': '1'
                }, {
                    duration: 500,
                    step: function(now, fx) {
                        if (fx.prop === 'opacity') {
                            $(this).css('transform', `translateY(${20 * (1-now)}px)`);
                        }
                    }
                });
            });

            // Inicializar DataTables con configuración mejorada
            const table = $('#proyectosTable').DataTable({
                dom: '<"row mb-3"<"col-md-6"l><"col-md-6"f>>rtip',
                language: {
                    url: '//cdn.datatables.net/plug-ins/1.13.7/i18n/es-ES.json',
                },
                pageLength: 10,
                order: [[0, 'desc']],
                responsive: true,
                stateSave: true,
                columnDefs: [
                    { orderable: false, targets: -1 },
                    { 
                        targets: 5,
                        render: function(data, type, row) {
                            if (type === 'display') {
                                const number = parseFloat(data.replace(/[^0-9.-]+/g,""));
                                return `<span class="fw-semibold">${new Intl.NumberFormat('es-CO', { 
                                    style: 'currency', 
                                    currency: 'COP',
                                    minimumFractionDigits: 0,
                                    maximumFractionDigits: 0
                                }).format(number)}</span>`;
                            }
                            return data;
                        }
                    }
                ],
                initComplete: function() {
                    $('.dataTables_wrapper').addClass('bg-white rounded');
                }
            });

            // Manejar el switch de proyectos inactivos
            $('#showInactiveProjects').on('change', function() {
                const showInactive = $(this).prop('checked');
                table.column(6).search(showInactive ? '' : 'activo').draw();
            });

            // Mejorar la experiencia de búsqueda
            const $searchInput = $('.dataTables_filter input');
            $searchInput
                .addClass('form-control-lg rounded-pill border-0 ps-4 pe-4')
                .attr('placeholder', 'Buscar en todos los proyectos...')
                .css({
                    'background': 'rgba(255,255,255,0.9)',
                    'box-shadow': 'var(--card-shadow)',
                    'width': '300px'
                });

            // Animación de iconos en hover
            $('.document-badge, .btn-icon').on('mouseenter', function() {
                $(this).find('i').addClass('fa-bounce');
            }).on('mouseleave', function() {
                $(this).find('i').removeClass('fa-bounce');
            });

            // Agregar efectos de paralaje suave
            $(window).on('scroll', function() {
                const scrolled = $(window).scrollTop();
                $('.page-header').css({
                    'transform': `translateY(${scrolled * 0.1}px)`,
                    'opacity': 1 - (scrolled * 0.003)
                });
            });
        });
    </script>
@endsection
